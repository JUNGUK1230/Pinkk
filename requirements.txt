numpy
opencv-contrib-python==4.12.0.88
# Ubuntu의 ROS/Matplotlib 바이너리는 NumPy 1.x ABI로 빌드되어 있습니다.
numpy<2
opencv-python
PyYAML
Flask>=3.0,<4
scipy
typeguard
# MPC horizon/state/input visualization. CasADi 3.8 removes an API used by
# do-mpc 5.1.1, so keep the verified compatible 3.7 series.
do-mpc==5.1.1
casadi>=3.7,<3.8
